/**
 * Design toggles you can flip in code.
 *
 * HERO_VARIANT switches the landing hero between two treatments that share
 * the same Bureau tokens (so both are theme-aware, light + dark):
 *   - "editorial" — broadsheet: serif display, italic emphasis, hairline
 *     rule, bordered uppercase CTA.
 *   - "bureau"    — grotesk display (Libre Franklin), slate accent word,
 *     solid primary CTA. Matches the rest of the app most closely.
 */
export const HERO_VARIANT: "editorial" | "bureau" = "editorial";
